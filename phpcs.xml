<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Laravel App Coding Standard">
    <description>PSR-12 coding standard for the Laravel application.</description>

    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <arg name="parallel" value="8"/>
    <arg value="sp"/>

    <!-- Paths to check -->
    <file>app</file>
    <file>modules</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>
    <exclude-pattern>*/_ide_helper*</exclude-pattern>

    <!-- PSR-12 Standard -->
    <rule ref="PSR12">
        <!-- Allow longer lines for readability in some cases -->
        <exclude name="Generic.Files.LineLength"/>
    </rule>

    <!-- Enforce line length with a higher limit -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="160"/>
        </properties>
    </rule>

    <!-- Forbid empty statements -->
    <rule ref="Generic.CodeAnalysis.EmptyStatement"/>

    <!-- Forbid unnecessary overriding of methods -->
    <rule ref="Generic.CodeAnalysis.UselessOverridingMethod"/>

    <!-- Forbid final methods in final classes -->
    <rule ref="Generic.CodeAnalysis.UnnecessaryFinalModifier"/>

    <!-- Detect commented-out code -->
    <rule ref="Squiz.PHP.CommentedOutCode">
        <properties>
            <property name="maxPercentage" value="40"/>
        </properties>
    </rule>
</ruleset>
